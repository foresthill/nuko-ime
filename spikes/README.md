# spikes/

短命の検証用 crate を置く。ワークスペース外。
本体の `Cargo.toml` には含めない (Cargo.lock を独立させる)。

## 一覧

- `libakaza-build/` — libakaza (akaza-im/akaza の MIT ライセンスのコア crate) が
  macOS でビルドできるかの spike。Path B 採用判断の根拠。
